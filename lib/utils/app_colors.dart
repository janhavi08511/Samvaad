import 'package:flutter/material.dart';

class AppColors {
  // Primary colors inferred from the design video (light mint/teal)
  static const Color primary = Color(0xFF66BB6A);
  static const Color accent = Color(0xFF81C784);

  // Backgrounds and Text
  static const Color background = Color(0xFFF5F7F9);
  static const Color darkText = Color(0xFF424242);
  static const Color greyText = Color(0xFF9E9E9E);
  static const Color lightGrey = Color(0xFFEEEEEE);
  static const Color white = Colors.white;

  // Mood colors (emojis are used primarily, but these can be for charts/indicators)
  static const Color happy = Color(0xFFFFCC80); // Light orange/yellow
  static const Color calm = Color(0xFF80CBC4); // Light teal
  static const Color sad = Color(0xFF90CAF9); // Light blue
  static const Color anxious = Color(0xFFEF9A9A); // Light red/pink
}
